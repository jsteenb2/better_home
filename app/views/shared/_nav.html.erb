<nav class="navbar navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>"><img alt="BetterHome" src="/white_brand.png"></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <div class='col-sm-8'>
        <% if !current_user.walk_score.nil? %>
            <%= simple_form_for current_user, :url => searches_path, method: :post, class: "navbar-form navbar-left" do |f| %>              
              <div class="row" id="search-bar">
                <div class='col-sm-4'>
                <%= f.text_field :location_id, as: :search, class: 'form-control', placeholder: "Search for Location " %> 
                </div>
                <div class='col-sm-2'>
                  <%= f.button :submit, value: "Search", class: "btn btn-success btn-block active" %>
                </div>
              </div>
              
            <% end %>

        <% end %>
      </div>
        
    
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name.capitalize %></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Account", edit_user_registration_path %></li>
            <li><%= link_to "Preferences", survey_user_path %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
